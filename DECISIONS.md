# DECISIONS.md

Every material decision is recorded here (and, where architectural, expanded as a MADR-style ADR
in `docs/adr/`). Silence is unacceptable: if it shaped the system, it is written down.

| # | Date | Decision | Rationale / consequence |
|---|---|---|---|
| D-001 | 2026-08-30 | Repository created as private `atqatq/sentinel`; work proceeds by micro-commits replayed from M0 | Owner requirement: incremental, plain-english history visible on GitHub |
| D-002 | 2026-08-30 | Repo layout follows delivery spec §3.2; module manifests follow build spec §14.15 (`sentinel.module.json`, registry, lifecycle) | The specs are the contract; repo structure must not invent a parallel architecture |
| D-003 | 2026-08-30 | Sanitization guard (`scripts/guards/forbidden-terms.sh`) blocks client brand, person, supplier names and secret-shaped strings in CI | Owner directive: no client-identifying mentions and no production data may ever be committed |
| D-004 | 2026-08-30 | `engine.js` / `feedback.js` ported **verbatim as JavaScript** first (117 golden tests green); TypeScript conversion with types-added/zero-logic-edits follows at M1 when `packages/config` tooling lands | Port and migration are separable failures; golden tests must be green before tooling churn |
| D-005 | 2026-08-30 | Micro-commit convention adopted (CONTRIBUTING.md): one logical change per commit, plain-english subjects, commit after every green state | Owner requirement: commits say what was written, deleted or improved |
| D-006 | 2026-08-30 | Execution boundary confirmed and encoded: Precoro executes; Sentinel plans, approves, verifies (build spec §14.7 transfers, §14.12 cycle counts, screen 15/16 read-only semantics) | Inventory staff work only in the system of record; Sentinel auto-reconciles from ingestion |
| D-007 | 2026-08-30 | Repository goes **public** (owner decision: no plan upgrade). Consequence: the tree *and the entire commit history* must be publishable — history was rewritten to sanitize early verbatim-port fixtures before the flip | Public content must satisfy the same data-governance rule as commits; the guard runs against a public surface |
| D-008 | 2026-08-30 | The guard assembles forbidden terms at runtime from string fragments; the trigger words appear nowhere in the repository, including in the guard itself | In a public repo the guard's own pattern list would leak the names it exists to protect; split fragments are inert |
| D-009 | 2026-08-30 | **No collaborators.** Any contribution requires an explicit, individual invitation from the repository owner; unsolicited PRs are not reviewed | Owner directive: all write access is granted personally, never granted broadly |
| D-010 | 2026-08-30 | SDS tokens ported verbatim from the design handoff README into `packages/ui/theme/tokens.css`; a parity gate parses the README tables and fails CI on any divergence. Structural tokens described only in prose (fonts, radii, shadows) carry an `--sds-` prefix where the natural name collides with a Tailwind theme namespace — without it, `@theme inline` emits circular `var()` references that invalidate the token | A12: SDS is the design source of truth, theme defaults never override the status language; the compile check caught the circular-emission failure mode live |
| D-011 | 2026-08-30 | Vendored shadcn/ui primitives bind their variant classes to SDS tokens (primary button = `--text` fill/`--inv` label, focus ring = `--info`, chrome = `--hover`/`--line-strong`); the one interpretive call — destructive buttons use `--critical` — is documented in `packages/ui/NOTICE.md` rather than left implicit. Primitives outside `packages/ui` are CI-blocked | A12 order: SDS wins over shadcn defaults; interpretive mappings must be reviewable, not silent |
| D-012 | 2026-08-30 | Ingestion v1 core lands as a pure module (`packages/core/modules/ingestion`, purity `no-db/no-react/no-framework/no-io/no-clock`): strict numeric parsing at the boundary — thousands separators, decimal commas, currency forms, scientific notation, booleans and non-finite numbers are stable *named* quarantine reasons, never coerced; the nz()-style fallback is legal at exactly one site (field declared optional **and** genuinely missing; present-but-corrupt optional values still quarantine); plausibility bounds are inclusive and the deliveries bounds-guard returns the full A5 plan in one value (quarantined record + trailing-7-day-mean baseline computed from valid entries only + `DATA_HEALTH` task + banner text naming the substitution; honest `NO_VALID_BASELINE` instead of an invented number). The **117-test canon (§15.4) remains engine+feedback**; per-milestone suites (this one: 32 tests) run as separate CI jobs until folded into the canon by a spec amendment | C4/A5: corrupt data coercing to zero is the spreadsheet era's defining failure; reason codes are contract for the quarantine UI and ledger; keeping the audit-regression canon frozen keeps counts comparable across audits; separate CI jobs avoid renaming required checks under branch protection |
| D-013 | 2026-08-30 | Screen 25 **Supplier Coverage** is retired as a standalone screen (owner accepted the designer's fold-in); duties attributed: suppliers by recipe reference → per-ref member SKU table, quotes → best-available baseline → Preferred SKU pin rationale. KPI **SRC-05 re-homes to a Supplier Scorecards header tile** with drill-down to the single-source category register. Screen IDs are stable — 25 retired, never renumbered | Amendment A15: the workflow was already covered by the designer's surfaces; the governance obligation (single-source exposure ≤ 15%, owner SBR, weekly) survives as SRC-05 with an explicit home instead of an orphaned source attribution; no new entity or ingest kind required |
| D-014 | 2026-08-30 | File-kind binding detects the kind by **header signature** and locates the header row as the first grid row fully matching a signature — instruction banner rows above it are stripped by position, never parsed as data. Signatures and alias maps are extracted from the shipped template's row-2 headers and pinned by `fixtures/golden/template_headers.json`, so template drift fails CI. Parentheses are real header content (`Ordered (Quantity)`); only square-bracket annotations are stripped. The first golden fixture corpus ships synthetic + checksum-pinned (H12 contract moves from empty to 4 pinned); real extracts swap in later behind the same checksums | §2/§3.1/§4: detection must survive Precoro's banner rows and annotated headers; the §2 discard list is enforced twice — absent from every alias map AND a hard `NEVER_KEPT` throw; fixtures stay synthetic until business-data approval |
| D-015 | 2026-08-30 | Normalization stage lands in the ingestion module with one philosophy — **refuse, don't guess**: unit spellings resolve against an *injected* tenant catalog (screen 32 data; nothing hardcoded) where unresolved spellings return `UNRESOLVED_UNIT` for data-health per the ingestion-spec stage order; openPO conversion happens **at ingestion** (`convertOpenPoRows`) with `MISSING_CONVERSION_FACTOR` matching engine R3's code and `INVALID_CONVERSION_FACTOR` for present-but-unusable (≤0/non-finite); money normalizes to tenant currency at ingestion storing `documentCurrency` + `tenantValue`, with the FX surface narrowed per Decision 7 to **local ↔ USD only** — a third currency is refused (`CURRENCY_NOT_SUPPORTED`), unpinned days refuse `RATE_NOT_PINNED`, and the rate direction is named in the data (`usdToLocalByDay`), not implied. `tenantCurrency` is mandatory (R1 mirror). Non-finite numerics reaching this stage throw — a wiring error, since parse.js guarantees survivors carry finite numbers | C1: ingestion must convert before openPO reaches the engine; C2: normalization at ingestion keeps mixed currencies from ever reaching KPI aggregation (the 10,000+10,000=20,000 poison); direction-named rates kill the inverted-FX defect class; the pinned-rate *service* (24h tenant-day pin, build spec decision 7) is app-layer and still owes its required ADR (delivery spec §102) before it lands |
| D-016 | 2026-08-30 | M1 data foundation lands in `packages/db`: `prisma/schema.prisma` is the typed table/column contract (19 models), while migrations are hand-written SQL carrying what Prisma cannot express — fail-closed RLS (ENABLE + FORCE + one `tenant_isolation` policy per tenant-scoped table, `current_setting('app.tenant_id', true)`), the `sentinel_app` role (`NOLOGIN NOBYPASSRLS`, DML-only grants), and H6/H7 structural keys (every idempotency UNIQUE leads with `tenant_id`; supplier `external_id` partial-unique when present, `(tenant_id, name)` interim). A drift test compares schema↔SQL both directions; a **live deny-matrix (37 checks) proves isolation on a real server** — both GUC failure modes (never-set → NULL → silent zero-row deny; set-then-reset → empty string → 22P02 loud deny), cross-tenant read/update/delete/write rejects, positive control, H6 no-collision across tenants, and FORCE binding a non-superuser owner. Proven locally on real PostgreSQL binaries and gated permanently in CI on `postgres:16`. Seed is synthetic-only (D-003). The Prisma client generation/consumption starts at M2 when `apps/web` exists | Delivery spec §2 mandates RLS as the enforcement point and an ADR for the model (ADR-0002); §5.2 demands the deny-matrix as an integration gate; §8 forbids floats for money (NUMERIC everywhere); audit era: missing tenant context must deny, never default open |
| D-017 | 2026-08-30 | H8 lands as a pure ingestion guard (`window.js`): strict UTC calendar-date parsing (round-trip validated), interval validation, and the normative invariant — the deliveries history must **cover** the consumption window (gap-walk inside the window, not extent matching; touching intervals merge; history may extend beyond the window). Refusals are named (`NO_DELIVERIES_HISTORY`, `INVALID_DELIVERIES_ENTRY`, `WINDOW_NOT_COVERED`, `INVALID_DELIVERIES_VALUE`, `END_BEFORE_START`) and carry a data-health task + banner naming the exact uncovered spans, mirroring the deliveriesGuard shape. `seedRateInputs` returns the guarded window total for the engine's `seedConsPerDelivery` — **the formula stays engine-owned, zero duplication**; entries crossing a window edge count at full value and are disclosed in `partialEdge` rather than silently prorated | Build spec §15.2 H8: a rate whose denominator spans a different window than its numerator is silently wrong and wrongs every plan derived from it; refusing a seed is an operational event, a wrong rate is a wrong steering decision; disclosure over invention for edge-crossing rows |
| D-018 | 2026-08-30 | H10 lands as a pure inbound-hardening gate (`hardening.js`) — the single choke point for every artifact: dropzone, watched-folder and email-in all ride `gateInboundFile`; the declared `source` is recorded but **never consulted** (proven by test), so email-in cannot have a softer path. Content is identified by **magic bytes, never by name**; the declared extension must agree with the content class (`DECLARED_MISMATCH` otherwise). The **XML family is refused outright** (`XML_REJECTED`) — XXE is made structurally impossible, not mitigated; if XML ever becomes a real input it lands behind a DTD-disabled parser with its own amendment. A ZIP must be an **XLSX workbook** (central directory carries `[Content_Types].xml` / `xl/*`) and passes **zip-bomb caps** before anything is inflated: entry count, inflated total, per-entry compression ratio above a 10 MiB floor (the floor protects small dense real XLSX parts), nested archives refused, ZIP64 refused at the boundary — caps are read from the central directory only, nothing is extracted. Formula injection is neutralized at the cell level with the **OWASP apostrophe escape** (`= + @ TAB CR`, and `-` only when not a plain canonical number); a neutralized cell that claimed a numeric column then fails the strict parser — hardening kills the execution vector, parse.js refuses the data. The **AV hook is fail-closed in every mode**: infected (`AV_INFECTED`), thrown (`AV_SCAN_ERROR`), malformed verdict (`AV_SCAN_ERROR`), and no-scanner-while-required (`AV_UNAVAILABLE`) — `avRequired` defaults to true, so production configures a scanner or the boundary refuses. Every refusal names a stable reason code, a DATA_HEALTH task and a banner — files are quarantined **whole** per the ingestion spec | Build spec §15.2 P1 H10 + delivery spec §9 A10 proofs (`ingest/magic-bytes`, `ingest/zip-bomb`, `ingest/formula-stripping`, M1 exit gate 13); a file that lies about its identity or inflates unbounded is the same defect class as `nz()` — silent at the boundary, catastrophic downstream; email-in-same-pipeline is enforced by construction, not by convention |
| D-019 | 2026-08-30 | **M1 exits at `0.2.0`.** Gates 3, 4 (fail-closed), 6, 12, 13 close with evidence recorded in `docs/milestones/M1-EXIT-REVIEW.md`: C1 conversion is called at ingestion (the audit's contract note is closed), C2 containment is fail-closed at the boundary (third currency and unpinned days refuse; `tenantCurrency` mandatory), C4's three named proofs are all green, H6's tenant-led keys are live-proven by the deny-matrix J-section, H7's Supplier ID identity is complete at schema + binding + spec level (R4 amended priority-1 [ADD]; the real column is a Precoro/cutover dependency), and H10's hardening gate is the single choke point with email-in proven identical. KPI-side named proofs (A1/A2) are explicitly scheduled with the KPI module in M2 — gate 4's M1 close is the fail-closed aspect per the milestone map, not a claim that the KPI tests exist. Housekeeping fixed at review: the RLS ADR is renumbered **ADR-0002** (it had collided with ADR-0001, the plugin-architecture ADR); H7's binding alias landed with the interim path tested, not assumed | Milestone → version map (delivery spec §6.3); the audit's "silence is unacceptable" rule — residuals are named and scheduled, never implied closed; stable ADR numbering is contract for every cross-reference in code comments, migrations and tests |
| D-020 | 2026-08-30 | The **kpi-catalog module** lands (§16, screen 34): all 28 KPIs defined once as data — id, definition, formula reference, source, owner, cadence, target — plus the **dataState envelope** every rendered KPI must wear: `OK`, `WITHHELD` (R1: mixed currencies null the six money metrics with the offending currencies named — an operational event, never a poisoned sum), `INSUFFICIENT_DATA` (R2: nothing plannable renders "insufficient plannable data" — never a fabricated 100%), `STALE` (seal older than the entry's explicit `staleAfterHours` renders stale **with the value kept** — never silent; time-based cadences carry explicit thresholds: daily 26h, weekly 182h, monthly 744h, hourly 2h; event-based cadences are never time-judged). The freshness stamp (`lastSealedAt`) and the portfolio's tenant-currency context are **mandatory** — absent either throws (R1 philosophy: optional tripwires fail open). The module **never re-implements a formula**: the portfolio strip maps the engine's verified canon into the envelope, an inconsistent portfolio (withheld flag + a money value present) throws, and the INV-04 grain difference (catalog text says SKU-days; the verified canon computes the share over plannable refs) is **disclosed on every result** — reconciling it owes a spec amendment, not a silent edit. INV-03's "below reorder point" composition (Zero Stock + Below Safety + Below Reorder) is likewise disclosed. A1/A2 named proofs close at the KPI layer: `kpi/tenant-currency-mandatory` · `kpi/mixed-currency-withholds-value` · `kpi/service-level-null-when-unplannable` (24 tests; gates 4 and 15 evidence) | Build spec §16 "defined once, as data" + §15.1 R1/R2; the withheld-KPI-is-an-operational-event rule; disclosure over invention for every canon-vs-text divergence |
| D-021 | 2026-08-30 | The **calendar module** lands — the H4 canonical temporal boundary and the H9 per-tenant working calendar (`packages/core/modules/calendar`, gates 10/A10). **H4**: the one canonical temporal form is date-only UTC `YYYY-MM-DD`; datetimes convert **at the boundary** with the **tenant timezone as an explicit tenant setting** (`{offsetMinutes}` or IANA via Intl — all GCC zones are fixed UTC+3, no DST); a naive datetime is runtime-ambiguous (the audit's E-confirmed defect: `new Date(string)` reads date-only as UTC but datetimes in the server's local zone, flipping `lateByDays` by a day across servers) and is therefore **refused as an instant** — the boundary accepts it only as tenant-local wall time via the explicit setting, recorded as `via: 'naive-local'`. Date math runs in **integer day units** — L-14's half-day rounding cannot occur. `feedback.js`'s `days()` switches to canonical day-unit math: date-only and zone-stamped inputs are deterministic on every server; a naive datetime arriving at the learning layer returns `null` (contract violation upstream, never re-guessed) — the 31 canon tests are byte-identical. **H9**: the day basis is **calendar-day actuals in, working-day calendar in between**: `WD` becomes per-tenant, per-period, calendar-derived (week pattern + merged closure intervals; unknown spec fields refused; flat calendars refuse date-range semantics), delivered through `computeRef`/`normalizeDeliveries` `opts.workingDays` — the SAME per-period wd on the input divisor and the magnification keeps §14.4b's identical-basis cancellation exact (monthly input magnifies to exactly rate × count on a closed month). **A flat-calendar tenant pins the workbook's 22 and is byte-identical to today's output** (named proofs `calendar/flat-tenant-identical`, `dates/canonical-boundary`; a 10-day Ramadan-style closure moves the per-working-day rate ×22/15 and run-out ×15/22). The engine's default path is untouched (86-canon intact); coarse granularities keep the constant construction with the period's wd substituted, and heterogeneous months should be entered monthly — disclosed, never silent | Build spec §15.2 P1 H4+H9 ("WD becomes calendar-derived, with a flat-calendar tenant byte-identical to today"); audit H4 [E-confirmed], H9, L-09 (WD/WK configurable → resolved by this calendar module), L-14 (`days()` rounding); delivery spec §9 A10 proofs `dates/canonical-boundary` · `calendar/*`; §14.4b identical-basis exactness; refuse-don't-guess for every ambiguous temporal value |
| D-022 | 2026-08-30 | The **plan-service module** lands — engine live wiring + sealed snapshots (`packages/plan-service`, M2). All database access is injected through **ports** (loader/saver); the module owns orchestration, never I/O, and is deterministic (asOf injected, no clock; rows sorted before hashing — identical inputs produce an identical seal). The **driver** (`deliveriesPerDay`) is request-carried per the deliveries-dashboard convention and normalized only by the engine's `normalizeDeliveries` — the service never re-derives it from history. Rate seeding rides the **H8 gate** (`seedRateInputs`): a ref with consumption whose window is uncovered refuses the **whole run** with the guard's named reason, gaps, index, task and banner untouched. Member aggregation uses the canon formulas verbatim (S = start+in−end−out via `reconcileConsumptionUnit`; T = S×CF via `toPlanningUnits`; RATE = `seedConsPerDelivery(T, histTotalDeliveries)`; histMonthly = T/window-months, the workbook `$U$1` analog) — **no engine formula is duplicated**. C1 discipline holds at planning time: open-PO rows without a converted quantity are excluded **and disclosed**; a member declaring a converted unit without a usable factor refuses the run (`UNCONVERTIBLE_MEMBER`, R3); identity conversion (item unit = planning unit) is disclosed. §14.4b's identical basis is structural: a real tenant calendar passes the same calendar-derived wd to BOTH the driver divisor and the magnification; flat/absent takes the engine's byte-identical default. The **seal** is one immutable row per tenant-day (`plan_seal`, UNIQUE tenant+seal_date — H6 in spirit): payload = full computed state + portfolio + dataState KPI envelope + disclosures + basis, hash = SHA256 over `canonicalJson` (sorted keys; RFC 8785 JCS arrives with H5/M3 and these hashes are expected to survive — divergence will be proven by the H5 vectors, never assumed). Replay returns the stored seal untouched; a **divergent same-day request is disclosed, never applied** (restatement is M8 semantics and does not exist yet). `ENGINE_VERSION` (engine module) and `SCHEMA_VERSION` (db package) are exported and stamped into every seal + column — the L-07 stamping obligation starts here; proposals/ledger/`/health` follow in their milestones. The API boundary (`handlePlanRun`) maps receipts to statuses: 200 sealed/replayed, 400 request-shape, 422 data-health refusal with task, 500 wiring error — transport (Next.js route handler) lands with the UI-shell unit. Public-contract `index.js` surfaces added to planning-engine, kpi-catalog, calendar and ingestion per ADR-0001 (cross-module access through the public surface only); module `main` fields pointed at them | Delivery spec §6.3 M2 "Engine live (computeRef + KPIs fail-closed)"; build spec §11 daily seal (full computed state + payload hash), §14.x deliveries-dashboard driver convention; §15.2 H8; §14.4b identical-basis; ADR-0001 manifest discipline; delivery spec §6.2 L-07 version stamping; the JSONB round-trip hash stability is proven live (plan-seal-live), not assumed |
| D-023 | 2026-08-30 | The **UI shell + status language** land with the `apps/web` scaffold (M2). **Status language**: the two-axis vocabulary is data in `packages/ui/src/status.ts` — inventory axis = the build-spec §3.1 seven PLUS the M1 display states (`Not Planned`, `No Lead Time` → `--warn`: data gaps that block planning while consumption continues, the README's "stale data" class — not `--muted`, which would visually bury the exact state M1 exists to surface); supply axis = §3.1's five (`Normal`→ok, `Follow-up with Supplier`→pending per the README token table, `Partial Delivery`→warn — the "MOQ shortfall" class, `Late PO`/`Supplier Issue`→critical — a broken supply commitment is a failure). Resolution is **fail-closed** (`UNKNOWN_STATUS` throws — an unbindable status never renders neutral) and the 53-check parity test binds the vocabulary to the REAL engine exports (`statusOf → displayStatus` walk, `supplyStatus` walk), so an engine-added state without a vocabulary update fails CI. The supply pill is the prototype's square form verbatim (radius 6, transparent, 34% border, square marker); screens may only render the axes through `InventoryStatus`/`SupplyStatus` — the new `ui/status-vocabulary-binding` guard bans raw-pill imports and hardcoded status literals in `apps/**` (scope is the rendering layer; the core modules legitimately own the ladder strings). **Shell**: the design README's top-bar menubar is implemented verbatim — 8 groups / 44 entries as data, hover-switching, EMPTY tags for every unbuilt screen (there is **no sidebar**: the README's explicit override of the brief's rail); wordmark, search trigger, tenant switcher, currency + density segmented controls, theme toggle, avatar, stale-data banner (caller supplies the facts — the clock belongs to M9's freshness layer), page header. The dropdown-menu is vendored shadcn/Radix (trimmed, NOTICE-documented adaptations) — a11y via Radix per delivery spec §8. **apps/web**: Next.js **15.5.24** (the spec pins the 15 major; 16 is not adopted silently) + React 19 + Tailwind 4 via `@theme inline`; `transpilePackages` ships the owned .tsx source. The scaffold page is a **named scaffold reference, not a §4 screen** — it fabricates no dataset. **HTTP transport**: `POST /api/plan` owns only transport — one transaction per run with a transaction-local `set_config('app.tenant_id', …, true)` (the ADR-0002 fence; the GUC dies with the transaction so pooled connections never leak a scope), injecting the pg adapter now extracted to `packages/db/plan-adapter.js` (single SQL source — the live proof and the app serve byte-identical queries; pg is lazy-required so no-database suites import the contract cleanly). Tenant identity stays request-carried as the declared interim; session-authenticated identity replaces it at the boundary when C3 SoD lands (M3) | Delivery spec §6.3 M2 ("UI shell + status language") + §8 (Next.js 15, Radix a11y, "status vocabulary binds only to displayStatus") + A12 gates (`ui/status-vocabulary-binding` now enforced); build spec §3.1/§3.2; design README §Layout shell/§Status/§Menubar (rendered-detail authority — including its no-sidebar decision); prototype `pill(label, tone, square)` for the two pill shapes; D-022's transport carve-out closed with the pg-adapter dedup; the handoff leaves the two M1 tones and all supply tones open — they are assigned here, reviewably, never silently |
| D-024 | 2026-08-30 | The **ops/freshness module** lands — the M9 freshness SLO + missing-deliveries alarm (`packages/core/modules/ops`, A11, gate 17, M2). DAT-01 is implemented as named constants **bound by test to the kpi-catalog target text** — the 26/36 bands are extracted from "≤ 26h; > 36h red + alarm" at test time, so spec drift fails CI, not production; band edges are inclusive, mirroring the C4 bounds convention (exactly 26h FRESH, exactly 36h DEGRADED). "Worst across file types" is computed over the **ingestion module's dataset kinds**, read through the public surface (ADR-0001) via a new additive `DATASET_KINDS` export sourced from the ingestion manifest itself — surface and manifest cannot drift, and the suite pins the exact eight kinds so a file type added without an ops-coverage review fails CI. A file type with **no seal ever is ALARM with a null age** — silence is never freshness, the pipeline-side twin of §16's "never a silent number" — and ANY unsealed kind holds DAT-01's value at null (a pipeline with a silent feed has no honest freshness number; it has an alarm). The **missing-deliveries channel is separate**, per M9's own title: deliveries is the demand primitive (ingestion spec: daily preferred, Blocking Yes, "no deliveries → the whole engine goes flat"), so the alarm fires when deliveries is anything but FRESH past the tenant's accepted cadence — a **tenant-amendable parameter** (default = the daily SLO; a weekly-cadence tenant passes 182) per §16's "targets are tenant-scoped and amendable per tenant without code change" — and its task/banner name the H8 refusal consequence, so DTA hears the silent feed **before** the morning run refuses, not from the refusal after the fact. Pure and deterministic: the clock is injected, seals validate fail-closed (`UNKNOWN_DATASET_KIND`, `FUTURE_SEAL`, `INVALID_SEALS`/`ENTRY`/`ASOF`/`DELIVERIES_CADENCE`), alarms are dataset-asc ordered and carry the window.js `DATA_HEALTH` task + banner conventions verbatim. The module is the **facts producer** for the kpi-catalog STALE envelope and the D-023 stale-data banner — whose clock carve-out was explicitly deferred to "M9's freshness layer"; the data-health screens render from these facts in their unit | Build spec §15.2 P2 M9 "freshness SLO and missing-deliveries alarm" + §16 DAT-01 and the freshness-stamp rule; delivery spec §9 A11 named spec `ops/freshness-alarm`, gate 17, §6.3 M2 row; ingestion spec deliveries row + §4; D-023's banner clock carve-out; the withheld-KPI-is-an-operational-event rule applied to the pipeline itself |
| D-025 | 2026-08-30 | The **Data Health screen** lands — the first §4 screen (`/data-health` + `GET /api/data-health`, M2 row "data-health screens"). The screen renders from **real facts only**: the M9 freshness envelope (D-024's obligation — "the data-health screens render from these facts in their unit") and the `data_health_task` register. Composition is layered per the established home rule — packages/db owns the SQL (`data-health-adapter.js`, the read-side single source), a **pure, deps-injected composition** owns semantics (the ops surface is injected, so the 15-test suite proves the envelope against the REAL module with no workspace install), `apps/web` owns transport, and ONE server reader feeds both the API route and the server component (the facts the API serves are byte-identical to the facts the screen renders). **DAT-01's "last successful per-tenant seal" maps to `max(applied_at)` over `APPLIED` ingest_file rows per kind** — a kind with no APPLIED row is null (never sealed), which the ops module turns into ALARM with a null age; the freshness panel is an addition to §9's anatomy (disclosed: the M2 row + D-024 obligate the rendering; the README §9 register remains authority for the register itself). The **stale-data banner renders from the freshness facts** — newest seal older than one day, the README shell rule in days, deliberately independent of DAT-01's 26h pipeline SLO — closing D-023's clock carve-out (dismissal is client state, deferred with the client-shell unit, disclosed). **Honesty rules as code**: `refsBlocked` is a number only when task payloads state it (otherwise null + disclosure — an under-count never impersonates a zero); an empty register is NOT evidence of health while H6 (the ingestion worker that persists DATA_HEALTH tasks) has not landed, so the screen distinguishes first-run empty (dataset named, received/applied difference stated), register-not-yet-recorded, and live-register states; unknown severity and unknown ingest kinds throw fail-closed; the banner timestamp renders in UTC wall format (the H4 lesson); the DAT-01 target travels as data from the ops constants (bound to the kpi-catalog text) so the render layer restates no spec number. **Tenant identity**: the screen/API take the tenant CODE (display identity) resolved to the fence uuid through the registry lookup — `tenant` is the registry above the fence by design; the transaction-local GUC fences every read (ADR-0002); authenticated identity replaces the interim at C3 (M3). The freshness-state tone binding (FRESH/DEGRADED/ALARM → ok/warn/critical) lives on the screen with a named promotion path: a second consumer moves it into packages/ui with a parity test, like the status vocabulary. The menubar's Data Health entry flips to `/data-health` — implemented items render as the link itself (Radix asChild), so a clickable entry can never carry an EMPTY tag | Design handoff §9 + §8 states family + §Layout shell; delivery spec §6.3 M2 row + §8 (BFF, server components, "no fabricated rows · no spinner · dataset named above"); build spec §16 DAT-01; D-022/D-023/D-024 precedents (transport-only routes, injected clock, facts producer); ADR-0001 (app consumption rides package public surfaces; ops gains `@sentinel/module-ops` + the DATASET_KINDS re-export bound by test) |
| D-026 | 2026-08-30 | The **H6 idempotent upsert wrapper** lands — the application-level replay no-op (M2 pipeline wiring, A7, the last scheduled M1 gate-12 residual, named proof `ingest/idempotent-per-tenant-replay`). The home rule splits it cleanly: the **pure decision layer** lives in the ingestion module (`src/idempotency.js`), the **SQL executor** in packages/db (`ingest-adapter.js`), and the future file-to-rows worker injects one behind the other. Keys derive per kind from INGESTION_FILE_SPEC §4's six-key list — with the H6 amendment carried **structurally, not string-prefixed**: the derived key is the row's BUSINESS identity (Item `SKU`; Inventory `SKU+Warehouse`; PO `PO#+SKU`; Supplier the H7 identity base `ext|Supplier ID` vs `name|Name` so an external-ID identity can never alias a name identity; Deliveries `period+granularity`; Params `RecipeRef`; consumption and category owners follow the schema's structural uniques) and the tenant dimension is the `idempotency_key` register's `tenant_id` column — exactly what migration.sql's H6 note means by "tenant-scoped by construction", and what makes the same fixture portable across tenants while their rows stay independent. Keys are `JSON.stringify` of whitespace-normalized parts (collision-proof, stable, printable enough for DAT-04 queries); case is preserved because code spelling is identity; date canonicalization stays the boundary's H4 obligation upstream and is not re-guessed here. **Deterministic collapse**: intra-file duplicate keys collapse last-occurrence-wins (equivalent to sequential upsert), order kept at first occurrence, the collapse disclosed in the plan — spec §4's "duplicate keys collapsed deterministically" made auditable. **The verdict**: a prior `ingest_file` row (the H6 unique `tenant+kind+checksum_sha256`) with status APPLIED → `REPLAY_NOOP` carrying the prior outcome's identity (fileId, appliedAt) — re-importing the same file changes nothing; any other prior status (RECEIVED/QUARANTINED/FAILED) → `APPLY` reprocessing THE SAME register row (the executor updates it in place — a retry never forks the file history). **DAT-04 honesty**: duplicateHits/newKeys are computed from the register contents loaded through a port (`loadSeenKeys`), never assumed from the verdict — a pre-wrapper file replays with honest zeros, and the ratio is the KPI layer's job (the screen restates no spec number). **Executor discipline**: all rows are pre-validated and all statements built BEFORE any write (a malformed row refuses the whole plan with zero statements issued — nothing half-applies even outside a transaction); the six wired kinds (items, suppliers two-branch H7, open_pos, consumption_balances, deliveries daily-only, planning_params) upsert `ON CONFLICT` on exactly the tenant-leading uniques the M1 structural fix created; the register write is `ON CONFLICT DO NOTHING` so a retry re-registers nothing; a `REPLAY_NOOP` plan is refused at the executor — it must never reach the database; non-daily delivery rows refuse `DELIVERIES_NON_DAILY_NOT_WIRED` (the engine reads daily; day-expansion for other granularities is a named future unit, never silent) and `inventory_all_dimensions`/`category_owners` refuse `KIND_NOT_WIRED` (mapping debt disclosed: inventory needs code→id resolution, category owners are control-plane identity — they wire with the full file-to-rows worker, not before). Fail-closed codes mirror the boundary's discipline: `MISSING_IDEMPOTENCY_KEY` (a row without its key can never be applied idempotently — the WHOLE file refuses), `INVALID_IDEMPOTENCY_KEY` (wrong type, never coerced), `UNKNOWN_DATASET_KIND`, `INVALID_CHECKSUM/MODE/PRIOR/SEEN`, `TENANT_MISMATCH`. Proven three ways in CI: 29 pure decision tests (ingestion job), 17 stub-client executor tests (db-rls job, no server), and 22 live checks against real PostgreSQL (per-tenant replay no-op with applied_at unchanged, cross-tenant independence, upsert-in-place with honest DAT-04, FAILED reprocess in place, H7 merge, RLS-fenced register) | INGESTION_FILE_SPEC §4 ("re-importing the same file changes nothing" + key list); build spec §15.2 H6 + §14.11; delivery spec A7/gate 12 (`ingest/idempotent-per-tenant-replay`) + §172 integration tier ("idempotency replay"); DAT-04; M1-EXIT-REVIEW residual table; migration.sql H6 structural note; D-016 (H7 identity), D-022 (ports discipline), D-024 (target-travels-as-data); ADR-0001 (surface), ADR-0002 (GUC fence) |
| D-027 | 2026-08-31 | **M2 exits at `0.3.0`.** Gates 15 and 17 — the milestone's assigned gates — close with evidence recorded in `docs/milestones/M2-EXIT-REVIEW.md`, and the review formally closes 7, 8, 9, 10 (carried re-audit PASSes + D-021's calendar delivery) plus the two scheduled M1 residuals that completed gates 4 and 12 (KPI-side named proofs via D-020; the application-level H6 wrapper via D-026). Gate 15's close is the whole layer: 28 KPIs defined once as data behind the fail-closed dataState envelope, R1 dead as a class (tenant-currency mandatory, mixed currencies withhold surgically, inconsistent envelope throws), R2 dead (null serviceLevel renders INSUFFICIENT_DATA — never a fabricated 100%), STALE explicit with mandatory freshness stamps. Gate 17's close binds DAT-01's bands to the kpi-catalog spec text at test time, makes silence an alarm (NO_SEAL_EVER, null age, no silent number), and gives missing deliveries its own tenant-amendable channel. The live tier earned its keep: the first CI run of the ingest-replay proof caught the explicit-NULL vs column-DEFAULT constraint class the stub suites cannot see — COALESCE at the SQL boundary, honest defaults named. Residuals are named and scheduled to M3 (worker wiring, C3, H5, M11, INV-04 spec amendment) — nothing implied closed | Milestone → version map (delivery spec §6.3); the audit's "silence is unacceptable" rule; M1-EXIT-REVIEW's residual-table discipline — every scheduled obligation lands or is re-scheduled with a named home |

| D-028 | 2026-08-31 | The **file-to-rows worker** lands — the production ingestion caller (M3 unit 1; the D-025/D-026 disclosed debt: "the H6 layers wait injectable", the two KIND_NOT_WIRED kinds, the non-daily deliveries day-expansion, the data_health_task persistence). The home rule holds: `packages/ingest-service` owns the pipeline semantics (ports-injected — no direct I/O; the caller owns the transaction and the GUC fence), `packages/db` owns the SQL (`ingest-worker-adapter.js` beside the executor), the pure ingestion stages stay the frozen canon it composes. **The pipeline**: dropzone / watched-folder / email-in all ride `runFileToRows` (the H10 gate is the single choke point; the source is recorded, never consulted — proven by a deep-equal receipt across sources) → strict decode → RFC-4180 grid (explicit delimiter; sniffing a dialect is a guess) → cell-level formula stripping → kind binding (instruction rows dropped, disclosed) → allow-list → **typed canonical rows**: every kept cell parses strictly or quarantines its ROW (the quarantine ledger carries the ORIGINAL file line; §4's "quarantined whole" governs the structural gates — binding, survivor-emptiness — while per-row corruption rides the ledger the schema ships) → units resolve against the tenant's injected catalog (unresolved = data-health item) → C1 openPO conversion at ingestion → C2 money at the pinned per-day rate (RATE_NOT_PINNED per row, never a guessed rate) → **day-expansion** (the D-026 named unit): weekly/monthly/quarterly rows spread over their period as EXACT-SUM integer-micro daily distributions (the last day absorbs the remainder — the H8 window total survives exactly; the smoothing is disclosed per §14.4b's own "coarse input smooths away day-to-day signal"), and **YTD refuses by name** (`YTD_CUMULATIVE_NOT_EXPANDABLE` — a running total spread over its period would double-count every earlier drop; the tenant drops monthly) → the A5 ±50% confirmation band (the worker owns the band check against the tenant's daily history port; the proven `deliveriesGuard` provides quarantine + trailing-7-day-mean substitution + task + banner) → inventory code→id resolution (SKU→item.id, warehouse code→warehouse.id — codes never reach the executor, `reqUuid` is the backstop) with the disclosed judgment that inventory value's document currency IS the tenant currency (the export carries no currency column; rate 1 LOCAL) → category-owner identity (email→user_id when the user exists; unknown email keeps owner_email with NULL user_id — honest control-plane identity) → the H6 decision (APPLY vs REPLAY_NOOP) → the proven executor. **Persistence honesty**: what the pure layers could only RETURN, the worker PERSISTS — QUARANTINED/FAILED register rows on the SAME H6 unique apply() upserts (a retry reprocesses in place, never forks history); pre-binding refusals (gate, workbook, grid, bind) write NO register row because the kind column never carries a guess — they live as CRITICAL data-health tasks; REPLAY_NOOP persists nothing (§4 verbatim); an APPLIED file's per-row quarantines land in `quarantine_record` with `ingest_file_id`, `quarantined_count` is stamped on the register row, and the guards' DATA_HEALTH tasks land in the register the /data-health screen reads (task severity: WARN floor, CRITICAL for whole-file refusals, fail-closed at the SQL boundary). **Named follow-on**: `XLSX_EXTRACTION_NOT_WIRED` — the gate accepts the workbook structurally (a crafted central-directory fixture proves the named refusal), but byte extraction needs a real workbook reader and no hand-rolled XML parser grows inside a pure module; tenants drop CSV exports or template tabs until it lands. **The suite earned its keep on first run**: the golden `items_modeA.csv` carries a row (TS-0003) with one extra comma — shifted one column from Brand onward — which no prior suite could see (file-binding reads HEADERS only); the worker's strict boundary quarantines that row per-row (SCIENTIFIC + UNRESOLVED_UNIT + FORMAT, line 6) instead of poisoning the file, and the honest counters (2 applied, 3 quarantined) are now pinned. Also fixed here: the db stub suites' runner was silently swallowing async-test failures (a bare `process.exit` before pending continuations) — both runners now await every pending assertion before the verdict, so all async pins genuinely run | INGESTION_FILE_SPEC §4 pipeline + §1 granularities; build spec §14.4b (divisors belong to the driver; coarse input, low confidence) + §14.8; delivery spec §6.3 M3; M2-EXIT-REVIEW residual table; D-025 (the screen's honest states name this unit), D-026 (the injectable layers + the two refused kinds + day-expansion deferral), D-024 (target-travels-as-data), D-022 (ports discipline, GUC fence), D-016 (H7 identity); ADR-0001 (surfaces), ADR-0002 (fence); OWASP CSV-injection stance via H10's stripFormulas |

| D-029 | 2026-08-31 | The **C3 financial-controls layer** lands — the SoD invariant, value tiers, dual control and the supplier-identity change freeze (M3 unit 2; gate 5; delivery-spec A4; audit C3 "the single most likely fraud vector"; build spec §15.2 P0-1). The home rule holds three ways: the **pure decision layer** is `packages/core/modules/approval` (manifest + public surface per ADR-0001; principals, tiers, prior votes and hold rows always injected — no clock, no I/O), the **SQL surface** is `0003_controls` + `procure-adapter.js`, and the two layers ENFORCE EVERY INVARIANT TWICE — the amendment's "API *and* RLS" pair, neither trusted alone. **The SoD invariant at the database** is a RESTRICTIVE RLS policy on `approval` (`sod_binding`): it ANDs with tenant isolation, binds every decision to the authenticated actor GUC `app.actor_id` (identity forging is structurally dead — you can only ever cast YOUR OWN approval), refuses the raiser via a proposal subquery (a forged cross-tenant proposal is invisible → NULL → denied — fail-closed), and requires an eligible role (O/SCM/SBR per the §10 capability row). Approval rows are append-only at RLS (UPDATE/DELETE denied) with reason NOT NULL (§16.2). **The state guard trigger** is the dual-control backstop: OPEN → APPROVED requires the tier's votes (1 at-or-below the tenant threshold, 2 strictly above), counted from qualified APPROVED rows joined to active roles and limits; refuses self-approval, ineligible votes, over-limit votes, totals that disagree with the lines, foreign currencies, and every transition the lifecycle does not name; a REJECTED decision dismisses in the same statement (re-raise as a NEW proposal — never revived). **Roles are regional data** (`tenant_role`, one active role per user × tenant, the six §10 codes); the tier config and per-role ceilings (`approval_config` dual threshold, `approval_limit`, NULL = Origin's unlimited) are tenant rows, Origin-amendable — the DEFAULTS are seed placeholders, never hardcoded control values, and grant/config/limit writes are Origin-only at RLS (`controls_origin_only` reads the actor's own O role back), with the first Origin bootstrapped through the migrator path because a policy that could self-grant would protect nothing. **The supplier-identity change freeze**: identity/remittance changes on `supplier` (external_id + name — the H7 identity bases — plus payment_term_days, payment_terms_text, currency_code, the Sentinel-visible surface; banking fields are discarded at ingestion) are refused outright by trigger; the ONLY door is a COOLING_OFF `supplier_change_hold` whose stored delta carries ALL five frozen fields (from/to, null-preserving — the apply path cannot smuggle an extra change), applied only under the transaction-local `app.hold_apply_id` with an EXACT row match, an out-of-band verification reference, an eligible verifier, and never the requester; the stored identity keeps serving throughout; the ingestion-path auto-staging (worker routes identity deltas into holds) is the named follow-on until wired — a direct change refuses fail-closed, never silently applies. **Refusal records are Class-D-shaped** (§16.2 semantic fields; the durable ledger write is H5, the very next M3 unit — the shape is fixed here so the ledger consumes it verbatim, no forked format). **The authenticated-identity boundary**: the module's principal ({userId, role}) is the session-resolved actor — the app.actor_id GUC extends the ADR-0002 fence with the same fail-closed construction (unset → NULL → denied; set-then-reset → '' → 22P02 loud); the request-carried tenant interim on the EXISTING plan/data-health routes retires when session issuance lands with M11 (disclosed, never silently dropped). Proven in CI: 35 pure tests including the FULL per-role-pair SoD matrix (the audit's "test per role pair") and the three A4 named proofs `sod/raisers-cannot-approve` (API+DB), `sod/dual-control-above-threshold`, `sod/supplier-change-freeze` — plus 14 stub-executor tests and the LIVE proof (sod-live.js, real PostgreSQL, 53 checks: policies deny by 42501, the guard and freeze triggers deny by named code, UNIQUE constraints make double votes and double conversions structurally impossible, RLS fences every new table cross-tenant, the actor GUC lifecycle is loud on reset). SCHEMA_VERSION 0002 → 0003; @sentinel/db 0.3.0 → 0.4.0 | Delivery spec §6.3 M3 + A4 (`sod/raisers-cannot-approve` · `sod/dual-control-above-threshold` · `sod/supplier-change-freeze`); gate 5 "SoD + dual control + thresholds in spec and code"; audit C3 (the self-approved-PO fraud vector; "test per role pair"); build spec §15.2 P0-1; ADR-0002 (the restrictive-policy extension it reserved for exactly this); M2-EXIT-REVIEW residual table; D-006 (execution boundary — conversion is a document of record, no Precoro write-back), D-016 (H7 identity bases name the frozen fields), D-022/D-023 (ports discipline, GUC fence, the identity-interim retirement schedule), D-025 (facts-only honesty), D-028 (home-rule unit shape) |
| D-030 | 2026-08-31 | The **H5 tamper-evident ledger** lands — RFC 8785 (JCS) canonicalization, the keyed HMAC-SHA256 hash chain, the Origin carve-out and the read-only verifier role (M3 unit 3; gate 11; delivery-spec A6; build spec §11 as amended by §15.2 item 3 + the whole §16 log scope). The home rule holds three ways: the **pure decision layer** is `packages/core/modules/ledger` (manifest + public surface per ADR-0001; the HMAC key is INJECTED — never read from env, never stored in code or database; the clock is injectable; no I/O), the **SQL surface** is `0004_ledger` + `ledger-adapter.js`, and the two layers enforce the posture together. **The canonicalization is RFC 8785**: `jcs.js` implements §3.2 normatively (code-unit key order, ES6 number serialization, §3.2.2.2 escapes, non-ASCII literal) and is pinned by checksum-pinned cross-implementation vectors (`fixtures/ledger/jcs-vectors.json` + SHA256SUMS — expected forms stated FROM the spec, never computed by the code; the vectors carry their own H12-style tamper check). **The D-022 survival obligation is PROVEN, not assumed**: for the seal-shaped and denial-shaped payloads the fixtures carry, `jcs(value)` is byte-identical to plan-service's `canonicalJson(value)` and the SHA256s match — the stored plan_seal hashes survive the JCS transition. **The hash** is `hex(HMAC-SHA256(key, seq ‖ prevHash ‖ canonicalJson(payload)))` — §11's construction kept verbatim, SHA256 upgraded to keyed HMAC; seq is concatenated, never inside the hashed JSON; prevHash is always exactly 64 lowercase hex (GENESIS = 64 zeros) so the concatenation is unambiguous; the key never leaves the application. **Every block, every class** (§16.1: W/A/N/S/D in ONE per-tenant sequence — class is a field, not a store) passes the §16.2 gate: the exact 17-field payload set (a foreign or undefined field refuses — undefined would drop silently from JSON; explicit null is the honest absence), denials REQUIRE a reason (CHECK-enforced at the DB too), `at` is canonical UTC millisecond precision (`canonicalInstant`; sub-millisecond REFUSES — the hash must round-trip from TIMESTAMPTZ(6), and the live proof walks that round-trip), a human principal carries its role, and a no-secrets scan (field-NAME blocklist, case-insensitive, any depth of before/after — refuse-known-bad-names, not content guessing; the ingestion boundary discards banking fields already) enforces §16.3 rule 3. **Immutability incl. Origin** is three independent layers, each proven live: SELECT+INSERT grants only for sentinel_app (any write is 42501-loud), RESTRICTIVE USING(false) UPDATE/DELETE policies (a role that HOLDS the grants still finds zero mutable rows — the honest silent-filter semantics), and `ledger_immutable` triggers that fire for anything reaching the table INCLUDING a superuser bypassing RLS (the §10 honest boundary verbatim: concealment holds against in-app roles, not direct infrastructure access). **The chain is structural**: composite PK (tenant_id, seq) + `ledger_chain_guard` trigger (seq must continue at 1 with GENESIS prev, else last+1 with the tail's hash — a wrong append raises LEDGER_SEQ_MUST_START_AT_ONE / LEDGER_GENESIS_PREV_HASH / LEDGER_SEQ_GAP / LEDGER_PREV_HASH_MISMATCH); the adapter serializes per-tenant appends with a transaction-scoped ADVISORY lock keyed by the tenant (`pg_advisory_xact_lock(hashtextextended(tenant_id))` — zero table privileges; the lock is its OWN statement before the tail read, because READ COMMITTED snapshots each statement at its start) — the original SELECT … FOR UPDATE demanded the UPDATE privilege immutability layer 1 must never grant and 42501'd on the first real append, live-caught by ledger-live's first CI run (racing genesis pairs and any residual snapshot race collide on the PK/chain guard, the loser retries — the chain never forks). **Deny-by-default is proven**: a forced ledger-write failure aborts the business transaction that rode the same tx (§16.3 rule 2). **The verifier** is the pure `verifyChain` walk (seq continuity, prev linkage, keyed-hash recomputation from the STORED payload fields, §16.2 re-validation; a break is REPORTED at its seq — never thrown) under the distinct NOLOGIN NOBYPASSRLS `sentinel_verifier` role (SELECT-only, cross-tenant by policy — that is the job). Tamper-evidence is demonstrated honestly: the live proof corrupts a hash via a disabled trigger (superuser-only path), the walk detects it at the exact block, restoration re-verifies. **The int8 lesson held a third time**: BIGINT seq is converted at the adapter boundary, pinned by a stub test. Refused mutations are THEMSELVES recorded (§16.4: origin-cannot-mutate's attempt block — Class-D, the refusal code as reason). Proven in CI: 50 pure module tests (vectors + gate + walk + the D-029 consumption proof — the REAL approval module's denial record travels through `denialToBlock` verbatim), 11 stub-adapter tests, 31 structural schema tests (9 new H5 pins), and the LIVE proof (ledger-live.js, real PostgreSQL: genesis/linkage/round-trip, all four chain-guard refusals, the three immutability layers, the recorded attempt, per-tenant genesis, five-class coverage with the export/system-actor shapes, denial-logged SOD_SELF_APPROVAL, no-secrets, write-failure-rolls-back, tamper detection + restoration, wrong-key detection, the verifier role cross-tenant read and its 42501 writes, checksum-pinned vectors). Named follow-ons (disclosed, not silent): **export-logged** and **lineage** (§16.4) wire when the export UI and the ingestion `importRunId` stamping land — the ledger consumes them without schema change; **key rotation** is a future amendment (the chain binds hashes, not key ids); the daily-seal Merkle anchor and the §11 time machine are milestone-scoped work beyond gate 11. SCHEMA_VERSION 0003 → 0004; @sentinel/db 0.4.0 → 0.5.0 | Delivery spec §6.3 M3 + A6 (`ledger/tamper-resistant` · `ledger/origin-cannot-mutate` · `ledger/jcs-vectors`) + §2 Ledger-crypto row; gate 11 "ledger keyed + canonicalization + Origin carve-out"; build spec §11, §15.2 item 3, §16.1–16.4; M2-EXIT-REVIEW residual table (the D-022 survival row); D-022 (canonicalJson + the survival expectation), D-029 (the Class-D shape consumed verbatim; the actor-GUC boundary), D-016/D-025/D-028 (precedents); ADR-0001 (surfaces), ADR-0002 (the GUC fence on the guard's tail lookup); H12 (checksum discipline, applied to the vectors themselves) |
| D-031 | 2026-08-31 | The **M11 authentication layer** lands — session issuance, the §14.9 floor for EVERY principal, the RFC 6238 TOTP second factor mandatory for approval-capable roles, the failed-login lockout for all accounts, and the retirement of the request-carried identity interim (M3 unit 4; gate M11; audit M11 fix "MFA mandatory for all approval-capable roles; session policy at least as strict as Origin's; lockout for all accounts"; named proofs `auth/mfa-approvals.spec` + `auth/session-policy.spec`). The home rule holds three ways: the **pure decision layer** is `packages/core/modules/auth` (policy floor, RFC 6238 TOTP with the Appendix-B SHA-1 vectors stated from the RFC text itself — the sixth vector was cross-checked against rfc-editor.org after a hand-copied memory value disagreed with a correct implementation, the H12 discipline applied to the paper — session lifecycle, lockout, password policy, sign-in machine; clock, entropy and attempt history always injected), the **SQL surface** is `0005_auth` + `auth-adapter.js`, and the two layers enforce the posture together. **The session floor is §14.9 verbatim for everyone**: 30-minute idle (DERIVED from last_seen_at — no column to drift; touch-on-read slides it), 8-hour absolute (pinned at issuance, never slides), termination as a tombstone (no DELETE grant for any actor). **The bearer token is stored ONLY as its SHA-256** (`token_hash` UNIQUE — a database read cannot mint a session); the raw token exists exactly once, in the issuance response. **The TOTP secret is WRAPPED AT REST** (AES-256-GCM, injected key — the H5 key posture: the key never touches the database); `last_used_step` is the replay guard with a ROW-LEVEL backstop (`UPDATE … WHERE last_used_step < $2` — a concurrent verifier of the same step finds zero rows), and the pure guard refuses an already-consumed step even inside the ±1 window. **MFA is enforced TWICE** (the D-029 API+DB pair): the pure `mayApprove` refuses an approval-capable principal (O/SCM/SBR — pinned by parity to the C3 authority) whose session lacks a verified factor, and the RESTRICTIVE `mfa_gate` policy on approval requires `app.mfa_ok = 'true'` — a never-set GUC reads NULL (refused), an EMPTY one (a reused session) reads '' (refused): fail-closed every shape. **A user with no TOTP enrolment still gets a session** (mfa_ok false) — enrolment needs a session (§14.10's forced enrolment runs in-app) — but the gate holds every vote until enrol+verify. **The GUC-EMPTY lesson, pinned empirically on PG16 and now load-bearing**: a transaction-local `set_config` leaves the GUC EMPTY on the session for its remaining life (after COMMIT, ROLLBACK and RESET alike — only a NEVER-touched GUC reads NULL), so every policy cast of an EMPTY GUC is 22P02-LOUD — fail-closed, never leaky; the verifier live proof sets a valid GUC in-tx and a production verifier connection is fresh (NULL path). **The interim is retired by name**: `POST /api/plan` refuses a body-carried `tenantId` (`SESSION_IDENTITY_REQUIRED`) and `GET /api/data-health` refuses `?tenant=` — identity comes from the session (httpOnly cookie → `user_session`), resolved once in `lib/auth-server` (the plan-adapter single-source rule), and the boundary sets the GUC trio `app.tenant_id` / `app.actor_id` / `app.mfa_ok` transaction-local. **The sign-in machine is composed in the adapter** (`attemptLogin` — the auth layer's ONE multi-statement atomic unit on a dedicated pool client: lockout → credentials → tenant binding → TOTP → decide → issue; the stateless challenge re-presents credentials, the server keeps no challenge state — D-031); the session's role IS the tenant_role it binds (the C3 principal), a user with no active tenant_role refuses `AUTH_NO_TENANT`, and the switcher (`PUT /api/auth/session/tenant`) moves a live session only where an active role (or Origin supremacy, logged like anyone else) allows. **Class-N lands in the chain**: session creation (and challenge failures) append through the ledger adapter INSIDE the caller's transaction — AWAITED, live-caught: a fire-and-forget statement on a shared connection interleaves protocol frames and reads GUC state from whatever transaction is current when the round-trip lands (the emission's tail SELECT raced the caller's ROLLBACK and read the EMPTY GUC as ''::uuid); pre-tenant failures (unknown email) live in `login_attempt` — the append-only pre-tenant record (SELECT, INSERT grants only, the ledger pattern). **RLS is honestly ABSENT on the four auth tables** (user_credential, mfa_enrolment, user_session, login_attempt): the session RESOLUTION precedes the tenant GUC — a tenant fence on the table that produces the fence's value is structurally impossible; reachable only through sentinel_app, token hashed, secret wrapped; the disclosure comment in 0005_auth is the record. **The scrypt deviation is named**: §10.1 mandates Argon2id/bcrypt ≥12 — the local-credential path uses node-native scrypt (N=2^15, r=8, 64B, maxmem raised explicitly) with zero native deps; PRODUCTION credentials live in the OIDC IdP (delivery spec AuthN/Z row), and the sign-in machine's credential step is shaped so an OIDC assertion replaces the local check without moving anything else — the IdP integration is the named follow-on `OIDC_IDP_NOT_WIRED` (with `FIDO2_NOT_WIRED` for passkeys, and the login UI screen as the named UI follow-on; the routes and the boundary are the M11 substance). Proven in CI: 27 pure module tests (the RFC vectors, the lifecycle matrix, the parity pin, both named proofs), 28 auth-adapter stub tests (statement shapes, wrap/unwrap, token-hash discipline, the composed flow), 37 structural schema tests (8 new M11 pins), and the LIVE proof (auth-live.js, real PostgreSQL, 28 checks: credential rotation, lockout over real rows, append-only 42501s, wrap-at-rest, replay refusal, touch-on-read, idle/absolute expiry by name, the tombstone, the mfa_gate named proof with 'true'/'false'/never-set, the Class-N block and the chain verify). SCHEMA_VERSION 0004 → 0005; @sentinel/db 0.5.0 → 0.6.0 | Audit M11 + §10.1 + §14.9 + §16.1 Class N; delivery spec §6.3 M3 gate M11 + the AuthN/Z row (OIDC posture); M2-EXIT-REVIEW residual table; D-023/D-029 (the interim retirement schedule + the actor-GUC boundary this unit completes); D-030 (the Class-N ledger surface consumed); D-022 (ports, injectable keys); ADR-0001 (surfaces), ADR-0002 (the GUC trio fence) |
